@model Centros.Model.Educador

@{
    ViewBag.Title = "Detalles de " + Model.ToString();
}

<h2>Detalles</h2>

<fieldset>
    <legend>Datos del educador</legend>

    @Html.DisplayForModel()

</fieldset>
<p>
    @Html.ActionLink("Modificar", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Volver a la lista", "Index")
</p>
